# Project Structure — Factory Console

Monorepo. One published wheel bundles the built SPA.

```
factory-console/
├── README.md                              # project pitch + quickstart
├── LICENSE
├── .gitignore
├── .python-version
├── pyproject.toml                         # T02 — Python build + deps + factory-console entry point
├── Dockerfile                             # T16 — multi-stage reproducible build
├── .dockerignore                          # T16
├── .pre-commit-config.yaml                # T05
├── Makefile                               # T09 — dev/test/build/package/smoke/release targets
│
├── .github/workflows/
│   ├── .gitkeep                           # tracked so the dir is committable
│   ├── ci.yml                             # T18 — matrix lint+test+build+smoke+e2e
│   └── release.yml                        # T26 — tag vX.Y.Z → PyPI via OIDC
│
├── docs/
│   ├── architecture.md                    # T19 — stub referencing docs/planning/ARCHITECTURE.md
│   ├── usage.md                           # T19
│   ├── contributing.md                    # T19 — dev loop, test loop, release
│   ├── screenshots/                       # T34 — README screenshots regenerated by Playwright
│   └── planning/
│       ├── VISION.md
│       ├── ARCHITECTURE.md                # durable backbone
│       ├── PROJECT_STRUCTURE.md           # this file
│       ├── ROADMAP.md                     # rolling-wave; MVP checklist + v1/v2 epics
│       ├── tickets.json                   # machine-readable manifest
│       └── tickets/mvp/                   # T01-T35 per-ticket plan files
│
├── server/
│   ├── README.md                          # subproject purpose + planned stack
│   └── factory_console/                   # imported as `factory_console`
│       ├── __init__.py                    # __version__          (T02)
│       ├── app.py                         # create_app + create_dev_app   (T06 → T20)
│       ├── cli.py                         # Typer entrypoint     (T06 → T25)
│       ├── config.py                      # pydantic-settings; host validator pinned to loopback   (T04)
│       ├── logging.py                     # request-log formatter + helpers                       (T04)
│       ├── errors.py                      # FactoryConsoleError base + to_error_response          (T04)
│       ├── api/
│       │   ├── __init__.py                # v1 router aggregator                                  (T20)
│       │   ├── deps.py                    # get_file_adapter Depends()                            (T20)
│       │   └── v1/
│       │       ├── __init__.py            # APIRouter(prefix='/api/v1')                           (T21)
│       │       ├── project.py             # GET /api/v1/project                                   (T21)
│       │       ├── projects.py            # /api/v1/projects registry reads + gated mutations     (T112-T113)
│       │       ├── tickets.py             # list + detail + deps handlers                         (T22-T23)
│       │       ├── roadmap.py             # GET /api/v1/roadmap (presence-only in MVP)            (T24)
│       │       └── health.py              # GET /api/v1/health enriched with projectRoot          (T24)
│       ├── domain/                        # Pydantic models — shared type surface                 (T07)
│       │   ├── __init__.py
│       │   ├── project.py
│       │   ├── ticket.py                  # exports TICKET_ID_PATTERN
│       │   ├── deps.py
│       │   ├── registry.py                # RegisteredProject + RegistryEntry(Condition)          (T103)
│       │   └── run_state.py
│       ├── services/                      # orchestrators; no I/O
│       │   ├── __init__.py
│       │   ├── ticket_service.py                                                                  (T22)
│       │   ├── deps_service.py                                                                    (T23)
│       │   ├── project_selection.py       # SelectionState + SelectionFailure + the precedence    (T111)
│       │   └── watcher_supervisor.py      # one watcher, re-rooted on a selection change          (T114)
│       ├── file_adapter/                  # the only layer that reads the TARGET PROJECT's files
│       │   ├── __init__.py
│       │   ├── protocol.py                # FileAdapter Protocol                                  (T10)
│       │   ├── fake.py                    # FakeFileAdapter                                       (T10)
│       │   ├── real.py                    # RealFileAdapter                                       (T17)
│       │   ├── discovery.py               # upward-walk + ProjectNotFound                         (T11)
│       │   ├── manifest.py                # tickets.json parser + MalformedManifest               (T12)
│       │   ├── ticket_md.py               # .md + front-matter parser + PathTraversal + TicketFileMissing (T13)
│       │   ├── markdown_render.py         # markdown-it-py + bleach sanitization                  (T14)
│       │   ├── project_condition.py       # ProjectConditionProbe + Real/Fake                     (T109)
│       │   └── run_state.py               # run-state prober (read-only)                          (T15)
│       ├── store/                         # the console's OWN writable DB (v3 — outside every project)
│       │   ├── __init__.py                # docstring-only, re-export-free                        (T104)
│       │   ├── location.py                # ConsoleStoreSettings + resolve_db_path + ensure_store_dir (T104)
│       │   └── entries.py                 # resolve_entries — rows × probe → RegistryEntry[]      (T110)
│       └── _static/                       # BUILT SPA copied here at package time (gitignored)
│
├── frontend/
│   ├── README.md
│   ├── package.json
│   ├── pnpm-lock.yaml
│   ├── svelte.config.js                   # adapter-static, SPA mode                              (T03)
│   ├── vite.config.ts                     # Vite dev + /api proxy                                 (T03)
│   ├── tailwind.config.js                                                                          (T03)
│   ├── postcss.config.cjs                                                                          (T03)
│   ├── tsconfig.json                      # strict                                                (T03)
│   ├── openapi-codegen.config.ts                                                                   (T28)
│   ├── playwright.config.ts                                                                        (T33)
│   ├── .eslintrc.cjs                                                                               (T03)
│   ├── .prettierrc                                                                                 (T03)
│   ├── .gitignore
│   ├── src/
│   │   ├── app.html                                                                                (T03)
│   │   ├── app.css                        # @tailwind directives + design tokens                  (T27)
│   │   ├── lib/
│   │   │   ├── api/
│   │   │   │   ├── client.ts              # thin fetch wrapper                                    (T28)
│   │   │   │   ├── types.ts               # GENERATED — DO NOT EDIT                               (T28)
│   │   │   │   ├── errors.ts              # ApiError                                              (T28)
│   │   │   │   └── index.ts               # re-exports                                            (T28)
│   │   │   └── components/                # StatusBadge, RunStateBadge, MarkdownBody, TicketRow, ChipList, FiltersBar, TicketMiniRow (T29-T32)
│   │   └── routes/
│   │       ├── +layout.svelte             # top bar + Reload                                      (T27)
│   │       ├── +layout.ts                                                                          (T27)
│   │       ├── +error.svelte              # global error boundary                                 (T27)
│   │       ├── +page.svelte               # ticket list                                            (T30)
│   │       ├── +page.ts                                                                            (T30)
│   │       ├── projects/                  # list / select / remove / add a project                (T123-T124)
│   │       │   ├── +page.svelte
│   │       │   └── +page.ts
│   │       └── tickets/[id]/
│   │           ├── +page.svelte           # detail                                                (T31)
│   │           ├── +page.ts                                                                        (T31)
│   │           └── deps/
│   │               ├── +page.svelte       # dep neighborhood                                      (T32)
│   │               └── +page.ts                                                                    (T32)
│   ├── scripts/
│   │   ├── postcodegen.mjs                # prepend "DO NOT EDIT" banner                          (T28)
│   │   └── copy-screenshots.mjs           # relocate to docs/screenshots/                         (T34)
│   └── tests/e2e/
│       ├── global-setup.ts                # boots packaged factory-console                        (T33)
│       ├── global-teardown.ts                                                                      (T33)
│       ├── happy-path.spec.ts             # MVP acceptance harness                                (T33)
│       └── screenshots.spec.ts            # captures README PNGs                                  (T34)
│
├── scripts/
│   ├── dev.sh                             # Uvicorn --reload + Vite proxy                        (T09)
│   └── package.sh                         # build SPA → copy to _static → build wheel            (T09)
│
└── tests/
    ├── unit/                              # file-adapter parsers, domain models, config, errors  (T04, T07, T10-T15)
    ├── integration/                       # /api/v1/* endpoints, CLI subprocess, RealFileAdapter (T20-T25)
    └── fixtures/
        ├── __init__.py
        └── projects/                      # minimal, with_run_state, malformed                    (T08)
            ├── README.md
            ├── minimal/
            ├── with_run_state/
            └── malformed/
```

## Track ownership

| Track | Owns |
|---|---|
| **foundation** | Everything above `server/factory_console/api|services|domain|file_adapter/` and above `frontend/src/`; CI + release + Dockerfile + scripts + Makefile + observability skeleton + docs stubs. |
| **file-adapter** | `server/factory_console/{domain,file_adapter}/` + `tests/fixtures/projects/`. The only layer that reads the TARGET PROJECT's files. |
| **backend** | `server/factory_console/{app.py, cli.py, api/, services/}`. Depends only on domain models + FileAdapter Protocol; never on `real.py` directly (except in `cli.py` and `create_dev_app`). Sanctioned exception: `api/deps._probe_root` (T111) does its own narrow readability stat/scandir of the selected project's path for the multi-project selection seam, rather than reusing `file_adapter`'s probing. |
| **frontend** | `frontend/`. Consumes REST v1 only; never renders markdown client-side (uses server-rendered `bodyHtml`). |
| **store** | `server/factory_console/store/` + `domain/registry.py`. The console's OWN durable state — the console DB, which lives outside every target project and is written by nothing else. |
| **github** | `server/factory_console/github_adapter/` + `domain/github.py` + `services/github_service.py`. Talks to the GitHub API only; never reads a target project's files. |

**`domain/` is shared vocabulary.** A track may ADD a new module there — named in its own ticket — but
may NOT edit another track's module there.
