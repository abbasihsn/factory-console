"""File-adapter layer: reads an App Factory project's files into domain models.

This is the first ``file_adapter`` module; later tickets add siblings (ticket
``.md`` rendering, project discovery, run-state probing). Kept docstring-only —
no re-exports — so those additions don't churn this file.
"""
